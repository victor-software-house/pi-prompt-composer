#!/usr/bin/env bash
#MISE description="Run Biome check --write on staged files"
#USAGE arg "files..." help="Files to format"

set -euo pipefail

if [ "$#" -eq 0 ]; then
  exit 0
fi

pnpm exec biome check --write --no-errors-on-unmatched "$@"
